# Test database overlay: adds a separate test DB so e2e tests don't
# clobber development data. Use: docker-compose -f docker-compose.yml -f docker-compose.test.yml up db
services:
  db:
    environment:
      POSTGRES_DB: reminder_db_test
